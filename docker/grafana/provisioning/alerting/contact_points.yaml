apiVersion: 1

# Discord contact point — configured via Grafana UI when DISCORD_WEBHOOK_URL is set.
# Navigate to Alerting > Contact points > Add, select Discord, paste webhook URL.
# Then set it as the default contact point under Alerting > Notification policies.
#
# Auto-provisioning is skipped because Grafana crashes if the webhook URL is empty.
contactPoints: []
